# Models Content

Generated by the Creation Engine (`Mikester9000/Creation-Engine`).
See `docs/ASSET_PIPELINE.md` for export commands.

Formats:
- `.obj` + `.mtl` — triangulated Wavefront OBJ meshes (vertices, normals, UVs)

To import:
    python tools/import_assets.py --engine creation --src /path/to/creation_export
