- 2026-05-25: Phase 1.2 plan audit + Tasks 053/054: Audited docs_ORDER_PLAN.md and ONE_FILE_COMPLETION_PLAN.md — added ✅/❌ status column to Phase 1.1 (orders 1-30) and Phase 1.2 (orders 31-80) tables in docs_ORDER_PLAN.md; updated Task 056 card with real READ_LINES (1-191). Implemented ORDER 48 (Task 053): created src/game/inventory/Equipment.hpp with EquipSlot enum, Equipment struct (atkBonus/defBonus/hpBonus/mpBonus/speedBonus), and EquipmentLoadout (4-slot array, Equip/Unequip, Total* aggregate helpers). Implemented ORDER 50 (Task 054): created src/game/progression/AbilityProgression.hpp + .cpp with Ability struct (id/displayName/unlockLevel/mpCost/cooldown/isPassive), AbilityProgression class (BuildDefaultTable with 12 abilities levels 1-15, NotifyLevelUp fires UnlockCallback per newly-unlocked ability, IsUnlocked/GetUnlocked query helpers). Equipment slot stub and Ability unlock flipped ✅ in SYSTEMS.md.
- 2026-05-24: Animation system implementation (Tasks 393-398H) — verified 58 hero_pack .anim clips (AnimEngine v1.0) in AssetRegistry; created Content/Animations/hero_pack/pack_manifest.json listing all 57 clip files with required clips present. Implemented src/game/animation/ module: AnimPackManifestLoader.hpp/.cpp (manifest load + required-clip validation), AnimClipLoader.hpp/.cpp (full keyframe/channel/event/root-motion parse via nlohmann/json), AnimationComponent.hpp/.cpp (per-actor playback state + crossfade + event polling), AnimationSystem.hpp/.cpp (CPU keyframe sampling with STEP/LINEAR slerp, 64-bone BoneTransformBuffer, blend-weighted output), PlayerAnimBridge.hpp/.cpp (PlayerActionState→clip name mapping, 0.20-0.30s crossfades), AnimEventDispatch.hpp/.cpp (routes footstep/contact/hit/cancel/cast_release events to tp::Audio + CombatSystem stubs). Added GetPlayerActionState()/GetCombatSystemMutable() accessors to RuntimeScene. Wired Main.cpp: startup builds heroClipLibrary from manifest, sets idle as initial clip; frame loop calls PlayerAnimBridge::Update → AnimationSystem::Advance → AnimEventDispatch::Dispatch each frame.
- 2026-05-24: CreationMaterialLoader — verified PR #56 content (61 materials.* entries in AssetRegistry.json, v1.1 PBR JSON files present across Content/Materials/). Implemented CreationMaterialLoader.hpp/.cpp: parses Creation-Engine material JSON v1.1 (name, shader, color[4], roughness, metallic, ao, emissive[3], six PBR texture paths). Handles legacy baseColor fallback and files-map fallback. Static cache keyed by material name; LoadDir uses std::filesystem recursive iterator. Wired Main.cpp to load all materials.* registry entries at startup (same pattern as animations.*).
- 2026-05-24: Audio import + Phase 1.2 combat-foundation tasks: Imported 46 audio assets from Mikester9000/Audio-Engine audio_export/ — created Content/Audio/import_manifest.json and registered all audio.* IDs in AssetRegistry.json. Extended tp::Audio with looping BGM/ambient (PlayBGM/StopBGM/PlayAmbient/StopAmbient via static ma_sound slots + MA_SOUND_FLAG_STREAM). Rewrote AudioManager with named gameplay hooks (PlayVictoryFanfare, PlayTacticalPauseEnter/Exit, PlayLockOnAcquire/Break, PlayEnemyAlertBark, PlayParrySFX). Added five consume methods to RuntimeScene (ConsumeEnemyDied, ConsumeEnemyAlert, ConsumeLockOnChanged, ConsumeParryOccurred) with enemy-death, alert-transition, and parry detection in BeginFrame. Wired all audio events in Main.cpp; changed startup BGM to bgm_field_day.ogg; added amb_forest_day_loop.wav ambient. Phase 1.2 gameplay: Added XP/level system (AddXp, kXpBasePerLevel × level scaling, maxHp+10/maxMp+5 per level-up) and StatusEffect (Poison/Burn/Slow) with StatusEffectState tick-based Update to PlayerStats.hpp; applied status tick damage each frame in RuntimeScene. Created CombatModifiers.hpp (CombatModifier + CombatModifierStack with DamageMultiplier/SpeedMultiplier/DefenseMultiplier, graceful-overflow Apply, Update, GetTotal* accessors). All completed systems flipped ✅ in SYSTEMS.md.
- 2026-05-23: Phase 1.2 Tasks 007/008/009: Task 007 — Enemy attack telegraph (full): strengthened EnemyActor wind-up pulse to a two-phase signal (slow moderate swell early, fast intense throb late), added `IsAttackTelegraphActive()` and `GetTelegraphPhase()` public accessors; Task 008 — Parry / counter window: added `parryWindowTimer` + `counterBonusActive` to PlayerActor, tight 0.12s parry window opens on every dodge start, successful parry in RuntimeScene absorbs hit and sets counter bonus, next attack step 1 fires at 2× damage via CombatSystem::SetNextHitMultiplier; Task 009 — Weak point damage: added `attackerYaw` to HitBox, CombatSystem checks cosine dot between attacker and enemy yaw in Update, 1.5× damage bonus when attacking from behind (cosAngle > 0.5); all three marked ✅ in SYSTEMS.md.
- 2026-05-23: Phase 1.2 Task 030: confirmed Phase 1.1 was already complete, then added minimap fog-of-war reveal by persisting visited cell coordinates in `Minimap` and rendering unvisited cells as darkened fog tiles until the player enters them; marked `Fog of war on minimap` ✅ in `docs/SYSTEMS.md`.
- 2026-05-23: Phase 1.1 Tasks 021-029: Marked tasks 021-024 (combat camera zoom, collision avoidance, target framing, lock-on recovery) ✅ in SYSTEMS.md — already implemented in CameraController. Implemented task 025 (tree wind sway via sinusoidal per-vertex offset in tree_vs.hlsl), task 026 (WeatherSystem — state machine with Clear/Cloudy/Rain/Storm transitions, ambient/wind/rain modifiers), task 027 (ParticleSystem — 48 ambient CPU particles drawn via ImGui background draw list), task 028 (DayNightCycle — accelerated 24h clock, sine-based sun angle, key-framed sky colors/ambient), task 029 (BiomeTransition — WorldGrid::NotifyBiomeChange + UpdateBiomeTransition, 2.5s cross-fade). All systems wired into Main.cpp frame loop.
- 2026-05-23: Phase 1 Tasks 002-004 + SYSTEMS.md fixes: Task 002 — enemy interrupt reaction now uses a longer hit-stagger (0.55s vs 0.30s) when the player breaks an attack wind-up, with distinct log tag [INTERRUPTED]; Task 003 — enemy model pulses (sinusoidal scale ±8%) during attack wind-up telegraph; Task 004 — brief red screen-edge damage flash (0.25s) triggers each time the player takes damage, wired through RuntimeScene::ConsumePlayerHitFlash() → GameHUD::TriggerDamageFlash(); SYSTEMS.md corrected: Camera shake, Hit pause/hitstop, Stagger meter, Enemy reaction/interrupt-lite, Enemy attack telegraph lite, and Screen edge damage flash all flipped ❌→✅; replaced former stubbed submenus with playable command actions (Basic Attack, Surge Strike, Limit Break), added readiness gating text for surge/limit rows, and wired all tactical commands to gameplay execution with consistent miss-indicator/SFX feedback.
- 2026-05-19: Policy compliance pass: removed Tactical Pause "Coming soon" stub command rows so the panel now exposes only implemented gameplay actions, and removed nonfunctional mouse-speed placeholder UI from Pause menu Options.
- 2026-05-17: Workflow hardening v2: added `worst_llm_guard.py doctor` preflight, enforced clean-worktree start gate, expanded deterministic docs to doctor/start/complete sequence, and appended mandatory end-of-road debugging/polish tasks (056-060).
- 2026-05-17: Workflow hardening: added `worst_llm_guard.py` with enforced task-scope and required-doc checks, and switched start/finish docs to guarded commands for first-generation local LLM reliability.
- 2026-05-13: Track 12.10: Added lightweight enemy hit flash blink, MISS indicator feedback, off-screen lock-on edge indicator, and Tactical Pause unavailable-command reason labels (Need Surge / No MP / Coming soon).
- 2026-05-13: Track 12.9: Improved lock-on flow with target cycling while locked and automatic nearest-target retargeting when a locked enemy dies or leaves valid range
- 2026-05-13: Track 12.8: Added Combat HUD expansion — FF7R-style target info panel (enemy name + colour-coded HP bar, bottom-centre) and combo step indicator (fading square dots with COMBO label) wired to lock-on target and active combo window; added EnemyActor::name field; tidied PLANNED_FEATURES.md checkboxes for all implemented Milestone 12 features
- 2026-05-13: Track 12.7b: Added Tactical Pause — hold Tab slows gameplay to 0.15x time scale and opens FF-style command panel (Surge Strike enabled when Surge full; Magic/Items/Ally stubbed as Coming soon); Tab stays reserved, lock-on remains on Q
- 2026-05-13: Docs: Added a concise copy-friendly Track 12.7b Wait Mode / Tactical Pause agent prompt and aligned Tactical Pause docs to keep lock-on on Q
- 2026-05-13: Track 12.7: Added header-only lock-on `Targeting` system (nearest-enemy toggle/validation), camera yaw bias toward locked target, lock-aware attack facing, lock marker overlay, and remapped LockOn from Tab to Q (Tab left free for Tactical Pause)
- 2026-05-13: Docs: Clarified Tactical Pause planning and moved battle music transition back to Milestone 15
- 2026-05-13: Docs: Lock in Surge / Limit Break / Tactical Pause / MP vision
- 2026-05-11: Docs: Added PLANNED_FEATURES.md master feature list and expanded COMBAT_VISION, UI_VISION, GRAPHICS_VISION, GAME_VISION_PLAN, and SYSTEMS with all planned feature suggestions
- 2026-05-07: Added `TextureCache` (DirectXTex-backed, SRV cache) and wired into `D3D11Renderer` terrain/ground draw paths.
- 2026-05-07: Added header-only `PlayerStats` and wired per-frame passive ATB charge updates through `RuntimeScene`.
- 2026-05-07: Added gameplay HUD overlay showing player HP/MP/ATB (with ATB READY indicator) and wired it into the ImGui frame path.
- 2026-05-07: Added single directional sun lighting (diffuse + ambient) with live debug-overlay sliders and shader updates for ground/basic3d/tree.
- 2026-05-08: Added header-only `InputActionMap` and migrated camera movement/jump plus Main.cpp Escape/F1/F5 edge polling to action-based bindings.
- 2026-05-08: Added simple custom static AABB collision (prefab-part blockers + camera movement pushout) to prevent player clipping through placed prefab geometry.
- 2026-05-08: Added basic EnemyActor with two-waypoint patrol, terrain-snap Y, and player-blockout visual; wired into RuntimeScene and Main.
- 2026-05-08: Added ImGui dialog box UI with typewriter text reveal, E-to-continue dismiss behavior, and main-loop wiring.
- 2026-05-11: Added action combat system: ATB-gated player attack (F key) spawns a temporary hitbox 1.5 units forward, dealing 3 damage to overlapping enemies via CombatSystem.
- 2026-05-11: Added `AudioManager` wrapper for tp::Audio with BGM/SFX playback hooks, pause-menu BGM volume slider, and startup/combat audio wiring.
- 2026-05-11: Added ImGui draw-list minimap overlay (top-right, 160x160) showing 5x5 nearby cells colored by biome, player position dot, and facing-direction arrow; hidden while pause menu is open.
- 2026-05-11: Added high-level vision docs: moved UI_VISION.md to docs/, added docs/GRAPHICS_VISION.md and docs/COMBAT_VISION.md, cross-referenced from GAME_VISION_PLAN.md.
- 2026-05-11: Track 12.1: Add PlayerActionState enum and state machine to PlayerActor
- 2026-05-11: Track 12.2: Add Dodge and LockOn actions to InputActionMap with default key bindings
- 2026-05-11: Track 12.3: Add dodge burst movement to CameraController (4 units over 0.35s, overrides WASD during burst)
- 2026-05-11: Track 12.4: Replace single ATB attack with 2-step combo chain (step 1: 3 dmg, step 2: 5 dmg, 0.60s window)
- 2026-05-12: Track 12.6: Fix ScreenProjection.hpp (replace manual rotation with DirectXMath matching renderer), add player AABB damage intake with enemy attack AABB check, stun-on-hit, defeat reset with camera respawn (CameraController::ResetToSpawn), and screen-edge low-HP warning pulse in GameHUD. Move DamageNumbers spawning from Main.cpp into RuntimeScene.
- 2026-05-12: Track 12.5: Add enemy combat state machine (Patrol/Chase/Attack/Hit/Dead) and debug hitbox/radius visualization toggle (F1 overlay)
- 2026-05-12: Added floating damage numbers with shared WorldToScreen helper (`ScreenProjection.hpp`), fixed-pool `DamageNumbers` HUD system, and combat-hit wiring.
- 2026-05-13: Track 12.6b: Surge system overhaul — basic attacks always free (no Surge gate), Surge fills from hits landed (0.30/0.40 per hit), Surge Strike (F when Surge full, 15 dmg), Limit Break gauge (fills 0.20 per hit taken), Limit Break move (Shift+F when Limit full, 25 dmg + 0.50 Surge bonus), MP passive regen (2.0/sec), LIMIT bar added to HUD (purple), PlayerStats::Reset() on respawn
- 2026-05-23: Task 001: Pressure/stagger integration — added Staggered state to EnemyState, pressure gauge (kPressurePerDamage=0.05/dmg, kPressureInterruptBonus=0.30 for Attack interrupts), 5s stagger duration with 1.6× damage multiplier, stagger visual scale (1.12×), and pressure/stagger bar in GameHUD target panel (amber fill → cyan when staggered).
- 2026-05-25: Completed all remaining not-started Phase 1.1 orders (12-18, 27-28). Added quality preset enforcement module (`src/app/QualityPreset.hpp/.cpp`), world streaming foundation modules (`src/world/WorldPartition`, `StreamingIO`, `WorldOriginRebase`, `WorldLodManager`, `StreamingBudget`, `ContinentImpostor`), and post-process toggle module (`src/renderer/PostProcessToggles.hpp/.cpp`) wired into pause menu options in `ImGuiLayer`. Registered all new files in `GameRewritten.vcxproj` + `.filters`; flipped Phase 1.1 statuses to ✅ in `docs/docs_ORDER_PLAN.md` and related `docs/SYSTEMS.md` rows.
